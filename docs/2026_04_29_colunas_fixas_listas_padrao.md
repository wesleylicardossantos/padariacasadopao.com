# Ajuste: títulos de colunas fixos nas listas padrão

Arquivo alterado:
- public/assets/css/saas-ui-system.css

Resumo:
- Aplicado CSS global para manter `thead th` fixo em tabelas dentro de `.table-responsive`.
- Incluído suporte para tabelas padrão SaaS (`.erp-saas-table`) e datatable legado (`.datatable-table .datatable-head .datatable-cell`).
- Sem alteração em controllers, rotas, models, migrations ou banco de dados.
